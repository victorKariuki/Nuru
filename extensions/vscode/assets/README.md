# Extension assets

Add `screenshot.png` here for the marketplace and README preview. The image is referenced in the main [README](../README.md) as `assets/screenshot.png` (relative to the `extensions/vscode` directory).
